# SBOM 架构

版权所有 © 2026 上海如静知华信息科技有限公司。

采集器从仓库、制品、镜像和固件生成 CycloneDX；组件服务按 PURL、版本和哈希归一；情报层匹配漏洞、利用和许可证；策略层形成 SLA 与发布决策；Spring Boot API 和 Vue 工作台承载处置闭环。

社区版提供组件风险评估规则。生产环境应保留不可变 SBOM 历史、制品签名和构建证明，并使用消息队列处理大规模扫描与情报重算。
