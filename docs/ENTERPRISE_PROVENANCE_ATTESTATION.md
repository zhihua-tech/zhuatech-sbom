# 企业级软件供应链来源证明

`POST /api/enterprise/sbom/provenance-attestation` 用于制品发布前的供应链可信校验。

系统检查 SBOM 完整性与签名、构建来源证明、依赖锁定、严重漏洞、许可证策略、例外豁免、可复现构建和制品摘要，并返回：

- `ATTEST`：证据链完整，可签发来源证明；
- `REVIEW`：无硬性缺口，但构建不可复现，需要人工核验；
- `BLOCKED`：存在安全、许可证或证据链阻断项。

生产环境应把签发结果、审批身份、制品摘要和策略版本写入不可篡改审计存储，并与 CI/CD 发布门禁集成。
